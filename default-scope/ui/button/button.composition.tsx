import { Button } from './button.js';

/**
 * @description Renders a basic button with the text "hello world!". It returns JSX
 * (JavaScript XML) that consists of a `<Button>` element, which is presumably defined
 * elsewhere in the codebase. The exact behavior and styling of the button depend on
 * the implementation of the `Button` component.
 * 
 * @returns {ReactNode} A JSX element, specifically a `<Button>` component with the
 * text "hello world!" as its content.
 */
export const BasicButton = () => {
  return (
    <Button>hello world!</Button>
  );
}
